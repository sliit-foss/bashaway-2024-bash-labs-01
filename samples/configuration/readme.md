## Dockerfile
```bash
consist of Instructions and arguments
Instructions are in CAPs
eg RUN , COPY ENTRYPOINT

docker is base on another image
RUN install dependencies
ENTRYPOINT - Run the command docker image run
docker create layers
docker history < image name >

docker will cache  changes
```